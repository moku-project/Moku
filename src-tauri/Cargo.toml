[package]
name = "moku"
version = "0.13.0"
edition = "2021"

[lib]
name = "moku_lib"
crate-type = ["cdylib", "rlib"]

[[bin]]
name = "moku"
path = "src/main.rs"

[build-dependencies]
tauri-build = { version = "2.0", features = [] }

[dependencies]
tauri                     = { version = "2.0", features = ["tray-icon"] }
tauri-plugin-shell        = "2"
tauri-plugin-process      = "2"
tauri-plugin-http = { version = "~2.5", default-features = false, features = ["rustls-tls"] }
reqwest           = { version = "0.12", default-features = false, features = ["rustls-tls"] }
tauri-plugin-dialog       = "2"
tauri-plugin-fs           = "2"
tauri-plugin-os           = "2.3.2"
tauri-plugin-store        = "2"
tauri-plugin-discord-rpc  = { git = "https://github.com/Youwes09/tauri-plugin-discord-rpc" }
serde                     = { version = "1", features = ["derive"] }
serde_json                = "1"
walkdir                   = "2"
sysinfo                   = "0.32"
dirs                      = "5"
font-kit                  = "0.14"
tokio                     = { version = "1", features = ["rt-multi-thread", "process", "io-util", "time", "sync"] }

[target.'cfg(unix)'.dependencies]
nix = { version = "0.29", features = ["signal", "process"] }

[target.'cfg(target_os = "windows")'.dependencies]
windows = { version = "0.58", features = [
    "Security_Credentials_UI",
    "Win32_UI_WindowsAndMessaging",
] }

[profile.release]
codegen-units = 1
lto           = true
opt-level     = "s"
panic         = "abort"
strip         = true
